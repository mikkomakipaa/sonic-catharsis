# Superseded

This file described the pre-redesign emotion-first model (an `EmotionWheel`
UI, and a subgenre picked purely by `(emotion, intensity)` with no model
judgment). Both are gone — replaced by the situation-first Trigger model and
the Matcher's sonic-profile architecture.

See instead:

- [`data-model.md`](./data-model.md) — the actual data shapes (`TriggerType`,
  `TriggerSelection`, Stage, sonic profile, Matcher/Curator contracts).
- [`formulas.md`](./formulas.md) — the Stage formula and the anchor genre
  table, including the history of two bugs found and fixed in each.
